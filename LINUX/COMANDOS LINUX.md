> **Comandos**
Mostrar kernel: <br>

```echo $0```
``````echo $SHELL``````
```tail /etc/passwd```
<br>


> **Ubuntu:**
* Listar pacotes: ```sudo dpkg  -l | grep nome_Pacote```

* Instalar pacote: ```sudo apt-get install nome_Pacote```
